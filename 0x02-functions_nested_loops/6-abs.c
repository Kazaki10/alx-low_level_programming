#include "main.h"
/**
 * _abs -  function that computes the absolute value of an integer.
 *
 * int _abs - function
 * @r: integer
 * Return: 0
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
	_putchar('\n');

}
